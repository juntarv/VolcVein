fallback: claude-design MCP unavailable — server announced as "still connecting" but never registered any tools; ToolSearch "select:mcp__claude-design__get_claude_design_prompt,mcp__claude-design__create_project,mcp__claude-design__write_files,mcp__claude-design__mint_handoff" returned "No matching deferred tools found" (only the "stitch" server and claude.ai connectors resolved). Screens were authored as self-contained HTML in design/html/ and rendered with render_html.sh / render_element.sh under the same tokens-first rules.
